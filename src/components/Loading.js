import React from 'react';
import { LoadingText } from '../styles/loading.sc';

const Loading = () => (
    <div className="col-lg-12">
        <LoadingText>Loading...</LoadingText>
    </div>
);

export default Loading;