import styled from 'styled-components';
import { colors } from './colors';

export const LoadingText = styled.p`
    text-align: center;
    color: ${colors.gray};
    font-weight: 500;
    font-size: 16px;
    padding: 12px;
    margin-bottom: 0;
`;